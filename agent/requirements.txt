# Web service (sub-step 3.1)
fastapi
uvicorn[standard]

# Config / env (sub-step 3.2)
python-dotenv

# LLM providers (sub-step 3.3)
langchain-groq
langchain-google-genai

# Agent loop (sub-step 3.4)
langgraph
